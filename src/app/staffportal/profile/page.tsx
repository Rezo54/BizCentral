'use client';
import Link from 'next/link';
import { useEffect,useState } from 'react';
import { useRouter } from 'next/navigation';
import { ArrowLeft,BadgeCheck,BriefcaseBusiness,Building2,CalendarDays,Camera,Hash,Loader2,Phone,ShieldCheck,UserRound } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { StaffPortalHeader } from '@/components/staff-portal-header';
type Profile={id:string;employeeCode:string;firstName:string;surname:string;occupation:string;businessName:string;edoId:string;idNumber:string;cellphone:string;appointmentDate:string;workWeek:string;status:string;profilePhotoUrl:string};
function date(v:string){if(!v)return 'Not recorded';const d=new Date(`${v}T12:00:00+02:00`);return Number.isNaN(d.getTime())?v:new Intl.DateTimeFormat('en-ZA',{day:'numeric',month:'long',year:'numeric'}).format(d)}
function Field({icon,label,value}:{icon:React.ReactNode;label:string;value:string}){return <div className="flex gap-3 rounded-xl border bg-background p-4"><div className="mt-0.5 text-muted-foreground">{icon}</div><div className="min-w-0"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-1 break-words font-medium">{value||'Not recorded'}</p></div></div>}
export default function ProfilePage(){const router=useRouter();const[p,setP]=useState<Profile|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');useEffect(()=>{let active=true;(async()=>{try{const r=await fetch('/api/staff/profile',{credentials:'include',cache:'no-store'});const j=await r.json();if(r.status===401){router.replace('/stafflogin');return}if(!r.ok||!j?.success)throw new Error(j?.message||'Unable to load profile.');if(active)setP(j.profile)}catch(e){if(active)setError(e instanceof Error?e.message:'Unable to load profile.')}finally{if(active)setLoading(false)}})();return()=>{active=false}},[router]);return <div className="min-h-screen bg-muted/30 pb-8"><StaffPortalHeader/><main className="mx-auto max-w-5xl px-4 py-6 sm:px-6"><Button variant="ghost" className="mb-4 px-0" asChild><Link href="/staffportal"><ArrowLeft className="mr-2 h-4 w-4"/>Back to Employee Portal</Link></Button><div><h1 className="text-2xl font-bold sm:text-3xl">My Profile</h1><p className="mt-1 text-sm text-muted-foreground">Your official employee information held by BizCentral.</p></div>{loading?<div className="mt-6 flex items-center gap-2 rounded-2xl border bg-background p-5"><Loader2 className="h-4 w-4 animate-spin"/>Loading profile...</div>:error?<div className="mt-6 rounded-xl border border-red-300 bg-red-50 p-4 text-sm text-red-700">{error}</div>:p&&<><section className="mt-6 rounded-2xl border bg-background p-5 shadow-sm"><div className="flex flex-col gap-5 sm:flex-row sm:items-center"><div className="relative flex h-24 w-24 shrink-0 items-center justify-center overflow-hidden rounded-full border-4 border-background bg-primary/10 text-primary shadow">{p.profilePhotoUrl?<img src={p.profilePhotoUrl} alt="Profile" className="h-full w-full object-cover"/>:<UserRound className="h-12 w-12"/>}<div className="absolute bottom-0 right-0 flex h-7 w-7 items-center justify-center rounded-full bg-slate-900 text-white"><Camera className="h-3.5 w-3.5"/></div></div><div className="flex-1"><p className="text-xs font-semibold uppercase tracking-wider text-muted-foreground">Employee Profile</p><h2 className="mt-1 text-2xl font-bold">{p.firstName} {p.surname}</h2><p className="mt-1 text-sm text-muted-foreground">{p.occupation||'Employee'} • {p.businessName}</p><div className="mt-3 flex flex-wrap gap-2"><span className="rounded-full bg-green-100 px-2.5 py-1 text-xs font-semibold text-green-800">{p.status||'Employed'}</span><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold text-slate-700">{p.workWeek==='6_day'?'6-day employee':'5-day employee'}</span></div></div></div></section><section className="mt-6"><h2 className="mb-3 font-semibold">Personal Details</h2><div className="grid gap-3 sm:grid-cols-2"><Field icon={<Hash className="h-4 w-4"/>} label="Employee Number" value={p.employeeCode}/><Field icon={<ShieldCheck className="h-4 w-4"/>} label="ID Number" value={p.idNumber}/><Field icon={<Phone className="h-4 w-4"/>} label="Cellphone" value={p.cellphone}/><Field icon={<BadgeCheck className="h-4 w-4"/>} label="Employee Status" value={p.status}/></div></section><section className="mt-6"><h2 className="mb-3 font-semibold">Employment Details</h2><div className="grid gap-3 sm:grid-cols-2"><Field icon={<Building2 className="h-4 w-4"/>} label="EDO Business" value={p.businessName}/><Field icon={<BriefcaseBusiness className="h-4 w-4"/>} label="Occupation" value={p.occupation}/><Field icon={<CalendarDays className="h-4 w-4"/>} label="Appointment Date" value={date(p.appointmentDate)}/><Field icon={<CalendarDays className="h-4 w-4"/>} label="Work Schedule" value={p.workWeek==='6_day'?'6-day employee':'5-day employee'}/></div></section><section className="mt-6 rounded-2xl border bg-background p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="font-semibold">Need to change something?</h2><p className="mt-1 max-w-2xl text-sm text-muted-foreground">Employee master information cannot be edited directly. Changes are submitted for review and only update your official record after approval.</p></div><Button disabled>Request Change</Button></div><p className="mt-3 text-xs text-muted-foreground">Change requests and profile-photo approval are the next profile step.</p></section></>}</main></div>}
