import {NextRequest,NextResponse} from 'next/server';import {FieldValue} from 'firebase-admin/firestore';import {getAdminAuth,getAdminDb} from '@/lib/firebase-admin';
async function context(req:NextRequest){const h=req.headers.get('authorization')||'';const token=h.startsWith('Bearer ')?h.slice(7):'';if(!token)throw new Error('Unauthorized');const decoded=await(await getAdminAuth()).verifyIdToken(token);const db=await getAdminDb();const snap=await db.collection('userAccess').doc(decoded.uid).get();if(!snap.exists)throw new Error('User access not found');return{db,uid:decoded.uid,access:snap.data()||{}}}
function idFor(name:string){return'edo-'+name.toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'')}
export async function POST(req:NextRequest){try{const{db,uid,access}=await context(req);if(String(access.userType||'').toLowerCase()!=='taskraft'||!['admin','superadmin'].includes(String(access.accessLevel||'').toLowerCase()))return NextResponse.json({error:'Admin access required'},{status:403});const body=await req.json();const month=String(body.month||'').trim();const records=Array.isArray(body.records)?body.records:[];if(!/^\d{4}-\d{2}$/.test(month)||!records.length)return NextResponse.json({error:'Month and records are required'},{status:400});const companies=await db.collection('companies').where('type','==','edo').get();const known=new Map(companies.docs.map(d=>[d.id,d.data()]));const batch=db.batch();let matched=0,unmatched=0;for(const raw of records){const registeredName=String(raw.registeredName||'').trim();if(!registeredName)continue;const guessed=idFor(registeredName);const companyId=known.has(guessed)?guessed:guessed;if(known.has(companyId))matched++;else unmatched++;const ref=db.collection('monthlyPerformance').doc(`${month}_${companyId}`);batch.set(ref,{...raw,month,companyId,registeredName,status:'draft',sourceFile:String(body.sourceFile||''),importedAt:FieldValue.serverTimestamp(),importedBy:uid},{merge:true})}await batch.commit();return NextResponse.json({ok:true,count:records.length,matched,unmatched,status:'draft'})}catch(e){console.error('Month-end import failed',e);return NextResponse.json({error:e instanceof Error?e.message:'Import failed'},{status:500})}}
export async function GET(req:NextRequest){try{const{db,access}=await context(req);const type=String(access.userType||'').toLowerCase();const url=new URL(req.url);const requestedMonth=url.searchParams.get('month');let q:any=db.collection('monthlyPerformance');if(type==='edo'){const companyId=String(access.companyId||access.edoId||'');if(!companyId)return NextResponse.json({error:'EDO company not linked'},{status:403});q=q.where('companyId','==',companyId).where('status','==','approved')}else if(type!=='taskraft')return NextResponse.json({error:'Not available for this account'},{status:403});if(requestedMonth)q=q.where('month','==',requestedMonth);const snap=await q.get();const rows=snap.docs.map((d:any)=>({id:d.id,...d.data()})).sort((a:any,b:any)=>String(b.month).localeCompare(String(a.month)));return NextResponse.json({ok:true,records:rows})}catch(e){console.error('Month-end read failed',e);return NextResponse.json({error:e instanceof Error?e.message:'Unable to load performance'},{status:500})}}
export async function PATCH(req:NextRequest){try{const{db,uid,access}=await context(req);if(String(access.userType||'').toLowerCase()!=='taskraft'||!['admin','superadmin'].includes(String(access.accessLevel||'').toLowerCase()))return NextResponse.json({error:'Admin access required'},{status:403});const body=await req.json();const month=String(body.month||'');const snap=await db.collection('monthlyPerformance').where('month','==',month).get();const batch=db.batch();snap.docs.forEach((d:any)=>batch.update(d.ref,{status:'approved',approvedAt:FieldValue.serverTimestamp(),approvedBy:uid}));await batch.commit();return NextResponse.json({ok:true,count:snap.size})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Approval failed'},{status:500})}}
