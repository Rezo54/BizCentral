'use client';
import {useEffect,useMemo,useState} from 'react';import {auth} from '@/lib/firebase';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';import {Button} from '@/components/ui/button';import {Eye,FileText,Loader2,Package,ReceiptText,Search,X} from 'lucide-react';import {Input} from '@/components/ui/input';
type Route={routeNo:string;breadQuantity?:number;breadRate?:number;breadAmount?:number;breadVat?:number;breadTotal?:number;repairs?:number};type StatementPart={creditNoteDetail?:string;invoiceDetail?:string;status?:string;description?:string;receiptDescription?:string;creditNoteNet?:number;creditNoteRunning?:number;invoiceNet?:number;invoiceRunning?:number;receiptNet?:number;closingBalance?:number};type Row={id:string;month:string;companyId:string;registeredName:string;contactPerson?:string;companyRegNo?:string;vatNo?:string;partnerCode?:string;purchaseOrder?:string;invoiceNo?:string;creditNoteNo?:string;invoiceDate?:any;dueDate?:any;cutOffDates?:string;routes?:Route[];squares?:any;fuel?:any;deductions?:any;billing?:any;statement?:{previous?:StatementPart;current?:StatementPart};bank?:any;status?:string};
const money=(v:any)=>`R ${Number(v||0).toLocaleString('en-ZA',{minimumFractionDigits:2,maximumFractionDigits:2})}`;const qty=(v:any)=>Number(v||0).toLocaleString('en-ZA',{minimumFractionDigits:2,maximumFractionDigits:2});const esc=(v:any)=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]||m));
function date(v:any){if(!v)return'';if(typeof v==='string'){const iso=v.match(/^\d{4}-\d{2}-\d{2}/);if(iso)return iso[0];const d=new Date(v);return Number.isNaN(d.getTime())?v:d.toISOString().slice(0,10)}if(v?.seconds)return new Date(v.seconds*1000).toISOString().slice(0,10);try{return new Date(v).toISOString().slice(0,10)}catch{return String(v)}}function statementDescription(r:Row,p:StatementPart,current=false){if(!current&&p.description)return String(p.description);const po=r.purchaseOrder||'';return [date(r.invoiceDate),po].filter(Boolean).join(': ')}
const css=`@page{size:A4;margin:0}*{box-sizing:border-box;-webkit-print-color-adjust:exact!important;print-color-adjust:exact!important}html,body{margin:0;padding:0;font-family:Arial,Helvetica,sans-serif;color:#111;background:#eee}.actions{width:210mm;margin:10px auto;display:flex;gap:8px}.actions button{border:0;border-radius:6px;background:#0879bd;color:white;padding:9px 14px;font-weight:700;cursor:pointer}.page{width:210mm;height:297mm;min-height:297mm;max-height:297mm;overflow:hidden;margin:0 auto;background:#fff;position:relative;padding-bottom:54mm;break-after:page;page-break-after:always}.page:last-child{break-after:auto;page-break-after:auto}.hero{height:55mm;background:#0879bd!important;color:white!important;padding:7mm 6mm;display:grid;grid-template-columns:1.08fr .92fr;gap:10mm}.hero h1{font-size:21px;line-height:1.45;margin:0;font-weight:700}.hero .type{font-size:21px;font-weight:700;margin-bottom:6mm}.company-extra{font-size:10px;line-height:1.5;margin-top:4mm}.docmeta{font-size:9px;line-height:1.45}.docmeta .row{display:grid;grid-template-columns:38mm 1fr}.main{padding:12mm 7mm 0}.twocol{display:grid;grid-template-columns:1fr 1.25fr;gap:25mm;align-items:start}.bill{font-size:11px;line-height:1.45}.bill b{font-size:11px}.shade{background:#f0f0f0!important;padding:1px 4px}.smalltable{border-collapse:collapse;width:100%;font-size:10px;margin-top:5mm}.smalltable td{border:1px solid #aaa;padding:2px 5px}.smalltable td:first-child{font-weight:700;width:40%}.callout{background:#92c7f2!important;padding:4mm;font-size:11px;margin-bottom:3mm}.items{border-collapse:collapse;width:100%;margin-top:10mm;font-size:10px}.items th{background:#4472c4!important;color:#fff!important;text-align:left;padding:2mm}.items td{border:1px solid #7da1d3;padding:3mm 2mm}.items tbody tr:nth-child(odd){background:#dbe5f1!important}.items tbody tr:nth-child(even){background:#f7f7f7!important}.items .totalrow td{background:#fff!important;border-top:0;font-weight:700}.right{text-align:right}.accept{font-size:10px;margin-top:16mm}.footer{position:absolute;left:0;right:0;bottom:0;height:47mm}.totalband{height:29mm;display:grid;grid-template-columns:68% 32%;color:#fff}.notes{background:#0879bd!important;padding:5mm 11mm;font-size:8px;line-height:1.35}.grand{background:#09abe0!important;padding:5mm 8mm;text-align:right}.grand .label{font-size:10px}.grand .amount{font-size:22px;margin-top:3mm}.powered{text-align:center;padding-top:5mm;font-size:10px}.powered .brand{font-size:20px;font-weight:800;color:#0781bf;margin-left:10mm}.powered .grey{font-size:20px;color:#aaa;font-style:italic}.statement .main{padding-top:11mm}.statement .items{margin-top:9mm}.age{background:#dbe5f1!important;text-align:center;font-weight:700;padding:1mm}.current{margin-top:4mm}.statement-total{display:flex;justify-content:flex-end;gap:42mm;font-weight:700;padding:4mm 8mm 0}.statement .footer{height:50mm}@media print{html,body{width:210mm;background:#fff!important}body{margin:0!important}.actions{display:none!important}.page{width:210mm!important;height:297mm!important;min-height:297mm!important;max-height:297mm!important;margin:0!important;overflow:hidden!important;break-inside:avoid!important;page-break-inside:avoid!important}.hero{height:52mm}.main{padding-top:9mm}.items{margin-top:7mm;font-size:9px}.items th{padding:1.6mm}.items td{padding:2.2mm 1.7mm}.accept{margin-top:10mm}.statement .main{padding-top:8mm}.statement .items{margin-top:6mm}.statement .age+.items{margin-top:0}.statement-total{padding-top:3mm}.smalltable{margin-top:4mm}.callout{padding:3mm}.footer{height:45mm}.statement .footer{height:47mm}}`;
function meta(r:Row,type:string,no?:string){return `<div><div class="type">${type}</div><div class="docmeta">${no?`<div class="row"><span>${type==='INVOICE'?'Invoice Number':'Document Number'}</span><span>${esc(no)}</span></div>`:''}<div class="row"><span>Contact Person</span><span>${esc(r.contactPerson||'')}</span></div><div class="row"><span>Registration Number</span><span>${esc(r.companyRegNo||'')}</span></div><div class="row"><span>Purchase Order</span><span>${esc(r.purchaseOrder||'')}</span></div><div class="row"><span>Vendor ID</span><span>${esc(r.partnerCode||'')}</span></div><div class="row"><span>${type==='INVOICE'?'Invoice Date':'Date'}</span><span>${date(r.invoiceDate)}</span></div>${type!=='STATEMENT'?`<div class="row"><span>Due Date</span><span>${date(r.dueDate)}</span></div>`:''}</div></div>`}
function hero(r:Row,type:string,no?:string,invoice=false){return `<div class="hero"><div><h1>${esc(r.registeredName)}</h1>${invoice?`<div class="company-extra">${r.vatNo?`VAT No: ${esc(r.vatNo)}<br>`:''}</div>`:''}</div>${meta(r,type,no)}</div>`}
function commonTop(r:Row,statement=false){return `<div class="twocol"><div class="bill"><b>Bill to:</b><div class="shade"><b>PREMIER FMCG (PTY) LTD</b></div><div>Building 5 Maxwell Office Park</div><div class="shade">Midrand</div><div>South Africa</div><table class="smalltable"><tr><td>Vat no:</td><td>4900114986</td></tr><tr><td>Contact:</td><td>Sandra Baker</td></tr><tr><td>Tel</td><td>012 492 7198</td></tr>${!statement&&r.cutOffDates?`<tr><td>Cut Off Dates</td><td>${esc(r.cutOffDates)}</td></tr>`:''}</table></div><div><div class="callout">${statement?`<b>As of: ${date(r.invoiceDate)}.</b> Please pay all arrears amount immediately! Please pay all the current amount within invoice terms`:'You can make payment to the following banking details:'}</div><table class="smalltable"><tr><td>Bank</td><td>${esc(r.bank?.bank||'')}</td></tr><tr><td>Branch Code</td><td>${esc(r.bank?.branchCode||'')}</td></tr><tr><td>Account Number</td><td>${esc(r.bank?.accountNumber||'')}</td></tr><tr><td>Payment Reference</td><td>${esc(r.bank?.paymentReference||r.invoiceNo||'')}</td></tr></table></div></div>`}
function footer(r:Row,total:any){return `<div class="footer"><div class="totalband"><div class="notes">Notes:<br><br>For any queries, please contact:<br>Benedict Mahlangu<br>082 373 7599<br>Benedict.mahlangu@taskraft.org</div><div class="grand"><div class="label">TOTAL</div><div class="amount">${money(total)}</div></div></div><div class="powered"><span class="grey">Powered by</span><span class="brand">TASKRAFT</span><br><br>This invoice was automated by Taskraft software. For more information visit taskraft.co.za</div></div>`}
function shell(title:string,page:string){return `<!doctype html><html><head><meta charset="utf-8"><meta name="color-scheme" content="light"><title>${esc(title)}</title><style>${css}</style></head><body><div class="actions"><button onclick="window.print()">Print / Save PDF</button></div>${page}</body></html>`}
function invoicePage(r:Row){const routeRows=(r.routes||[]).map((x,i)=>`<tr><td>Service Fee (Bread): ${i?'@':'Per'} Net Loaves sold: ${esc(x.routeNo)}</td><td>${qty(x.breadQuantity)}</td><td>${money(x.breadRate)}</td><td>${money(x.breadAmount)}</td><td>${money(x.breadVat)}</td><td>${money(x.breadTotal)}</td></tr>`).join('');const sq=`<tr><td>Service Fee (Squares): @Per ${r.routes?.length===1?'Net Loaves':'pack'} sold</td><td>${qty(r.squares?.quantity)}</td><td>${money(r.squares?.rate)}</td><td>${money(r.squares?.amount)}</td><td>${money(r.squares?.vat)}</td><td>${money(r.squares?.total)}</td></tr>`;return `<section class="page">${hero(r,'INVOICE',r.invoiceNo,true)}<div class="main">${commonTop(r)}<table class="items"><thead><tr><th>Item Description</th><th>Quantity</th><th>Unit Price</th><th>Amount</th><th>Vat</th><th>Total</th></tr></thead><tbody>${routeRows}${sq}<tr><td>Other</td><td>0.00</td><td>${money(0)}</td><td>${money(0)}</td><td>${money(0)}</td><td>${money(0)}</td></tr><tr class="totalrow"><td colspan="3" class="right">TOTAL</td><td>${money(r.billing?.invoiceExVat)}</td><td>${money(r.billing?.invoiceVat)}</td><td>${money(r.billing?.invoiceTotal)}</td></tr></tbody></table><div class="accept">Accepted by _________________________ &nbsp; Sign __________________ &nbsp; Date __________________</div></div>${footer(r,r.billing?.invoiceTotal)}</section>`}
function creditPage(r:Row){const d=r.deductions||{};const repairRows=(r.routes||[]).map((x,i)=>`<tr><td>Repairs & Maintenance:${i<(r.routes||[]).length-1?' Kitty':''} ${esc(x.routeNo)}</td><td>${Number(x.repairs||0)!==0?'1.00':'0.00'}</td><td>${money(x.repairs)}</td><td>${money(x.repairs)}</td><td>${money(0)}</td><td>${money(x.repairs)}</td></tr>`).join('');const rows=`<tr><td>Diesel Recharges: Per Litre</td><td>${qty(r.fuel?.litres)}</td><td>${money(r.fuel?.rate)}</td><td>${money(r.fuel?.amount)}</td><td>${money(0)}</td><td>${money(r.fuel?.amount)}</td></tr>${repairRows}<tr><td>Kitty Saving</td><td>${Number(d.kitty||0)?'1.00':'0.00'}</td><td>${money(d.kitty)}</td><td>${money(d.kitty)}</td><td>${money(0)}</td><td>${money(d.kitty)}</td></tr><tr><td>Truck Lease Repayment</td><td>${Number(d.truckLease||0)?'1.00':'0.00'}</td><td>${money(d.truckLease)}</td><td>${money(d.truckLease)}</td><td>${money(0)}</td><td>${money(d.truckLease)}</td></tr><tr><td>Crate Shortage</td><td>${Number(d.crates||0)?'1.00':'0.00'}</td><td>${money(d.crates)}</td><td>${money(d.crates)}</td><td>${money(0)}</td><td>${money(d.crates)}</td></tr><tr><td>Outstanding Other: AOD</td><td>${Number(d.aod||0)?'1.00':'0.00'}</td><td>${money(d.aod)}</td><td>${money(d.aod)}</td><td>${money(0)}</td><td>${money(d.aod)}</td></tr><tr><td>Dolley Shorts</td><td>${Number(d.dolleys||0)?'1.00':'0.00'}</td><td>${money(d.dolleys)}</td><td>${money(d.dolleys)}</td><td>${money(0)}</td><td>${money(d.dolleys)}</td></tr>`;return `<section class="page">${hero(r,'CREDIT NOTE',r.creditNoteNo)}<div class="main">${commonTop(r)}<table class="items"><thead><tr><th>Item Description</th><th>Quantity</th><th>Unit Price</th><th>Amount</th><th>Vat</th><th>Total</th></tr></thead><tbody>${rows}<tr class="totalrow"><td colspan="3" class="right">TOTAL</td><td>${money(r.billing?.creditExVat)}</td><td>${money(r.billing?.creditVat)}</td><td>${money(r.billing?.creditTotal)}</td></tr></tbody></table></div>${footer(r,r.billing?.creditTotal)}</section>`}
function statementRows(r:Row,p:StatementPart,current=false){const desc=statementDescription(r,p,current);return `<tr><td>${esc(p.creditNoteDetail||'')}</td><td>Sales Credit Note</td><td>${esc(p.status||(current?'Open':'Closed'))}</td><td>${esc(desc)}</td><td>${money(p.creditNoteNet)}</td><td>${money(p.creditNoteRunning)}</td></tr><tr><td>${esc(p.invoiceDetail||'')}</td><td>Sales Invoice</td><td>${esc(p.status||(current?'Open':'Closed'))}</td><td>${esc(desc)}</td><td>${money(p.invoiceNet)}</td><td>${money(p.invoiceRunning)}</td></tr>`}
function statementPage(r:Row){const p=r.statement?.previous||{},c=r.statement?.current||{};return `<section class="page statement">${hero(r,'STATEMENT')}<div class="main">${commonTop(r,true)}<table class="items"><thead><tr><th>Doc/Age</th><th>Transaction Type</th><th>Status</th><th>Description</th><th>Net Amount</th><th>Running Balance</th></tr></thead></table><div class="age">1 MONTH</div><table class="items" style="margin-top:0"><tbody>${statementRows(r,p)}<tr><td></td><td>Receipt</td><td></td><td>${esc(date(p.receiptDescription))}</td><td>${money(p.receiptNet)}</td><td></td></tr></tbody></table><div class="age">1 MONTH <span style="float:right;margin-right:16%">${money(p.closingBalance)}</span></div><div class="age current">CURRENT</div><table class="items" style="margin-top:0"><tbody>${statementRows(r,c,true)}</tbody></table><div class="statement-total"><span>CURRENT</span><span>${money(c.closingBalance??r.billing?.statementCurrent)}</span></div></div>${footer(r,c.closingBalance??r.billing?.statementCurrent)}</section>`}
function openHtml(html:string,title:string){const w=window.open('','_blank');if(!w)return alert('Pop-up blocked. Allow pop-ups for BizCentral to view documents.');w.document.open();w.document.write(shell(title,html));w.document.close()}
function openDoc(r:Row,type:'invoice'|'credit'|'statement'){openHtml(type==='invoice'?invoicePage(r):type==='credit'?creditPage(r):statementPage(r),`${r.registeredName} ${type}`)}function fullPack(r:Row){openHtml(`${invoicePage(r)}${creditPage(r)}${statementPage(r)}`,`${r.registeredName} ${r.month} Billing Pack`)}
export default function MonthEndInvoicePage(){const[rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState(''),[month,setMonth]=useState(''),[search,setSearch]=useState(''),[selected,setSelected]=useState<Row|null>(null);useEffect(()=>{(async()=>{try{const u=auth.currentUser;if(!u)throw new Error('Please sign in again');const token=await u.getIdToken();const res=await fetch('/api/month-end',{headers:{Authorization:`Bearer ${token}`},cache:'no-store'});const x=await res.json();if(!res.ok)throw new Error(x.error||'Unable to load month-end billing');setRows(x.records||[]);if(x.records?.length)setMonth(x.records[0].month)}catch(e){setError(e instanceof Error?e.message:'Unable to load billing')}finally{setLoading(false)}})()},[]);const months=useMemo(()=>[...new Set(rows.map(x=>x.month))].sort().reverse(),[rows]);const shown=useMemo(()=>rows.filter(x=>(!month||x.month===month)&&(!search||x.registeredName.toLowerCase().includes(search.toLowerCase())||(x.routes||[]).some(r=>r.routeNo.toLowerCase().includes(search.toLowerCase())))),[rows,month,search]);if(loading)return <div className="flex min-h-64 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin"/></div>;return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Month-End Invoice Packs</h1><p className="text-muted-foreground">Review invoice, credit note and statement in the original EDO billing layout. Print / Save PDF downloads the document as PDF.</p></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-4 text-red-700">{error}</div>}<Card><CardContent className="pt-6"><div className="grid gap-3 md:grid-cols-[220px_1fr]"><select className="h-10 rounded-md border bg-background px-3 text-sm" value={month} onChange={e=>setMonth(e.target.value)}>{months.map(m=><option key={m}>{m}</option>)}</select><div className="relative"><Search className="absolute left-3 top-3 h-4 w-4 text-muted-foreground"/><Input className="pl-9" placeholder="Search EDO or route" value={search} onChange={e=>setSearch(e.target.value)}/></div></div></CardContent></Card><div className="space-y-3">{shown.map(r=><Card key={r.id}><CardHeader className="pb-2"><div className="flex flex-col gap-2 md:flex-row md:items-center md:justify-between"><div><CardTitle className="text-lg">{r.registeredName}</CardTitle><p className="text-sm text-muted-foreground">{(r.routes||[]).map(x=>x.routeNo).join(' • ')} · {r.month} · {r.status||'draft'}</p></div><div className="text-sm font-semibold">Net statement: {money(r.billing?.statementCurrent)}</div></div></CardHeader><CardContent><div className="grid gap-3 md:grid-cols-3"><div className="rounded-xl border p-4"><ReceiptText className="h-4 w-4"/><p className="mt-2 font-semibold">Invoice</p><p className="text-xl font-bold">{money(r.billing?.invoiceTotal)}</p><p className="text-xs text-muted-foreground">{r.invoiceNo||'Invoice number pending'}</p><Button className="mt-3 w-full" variant="outline" onClick={()=>openDoc(r,'invoice')}><Eye className="mr-2 h-4 w-4"/>View / Download</Button></div><div className="rounded-xl border p-4"><FileText className="h-4 w-4"/><p className="mt-2 font-semibold">Credit Note</p><p className="text-xl font-bold">{money(r.billing?.creditTotal)}</p><p className="text-xs text-muted-foreground">{r.creditNoteNo||'Credit note number pending'}</p><Button className="mt-3 w-full" variant="outline" onClick={()=>openDoc(r,'credit')}><Eye className="mr-2 h-4 w-4"/>View / Download</Button></div><div className="rounded-xl border p-4"><FileText className="h-4 w-4"/><p className="mt-2 font-semibold">Statement</p><p className="text-xl font-bold">{money(r.billing?.statementCurrent)}</p><p className="text-xs text-muted-foreground">Includes 1 MONTH + CURRENT</p><Button className="mt-3 w-full" variant="outline" onClick={()=>openDoc(r,'statement')}><Eye className="mr-2 h-4 w-4"/>View / Download</Button></div></div><div className="mt-3 flex flex-wrap gap-2"><Button onClick={()=>fullPack(r)}><Package className="mr-2 h-4 w-4"/>Full Billing Pack</Button><Button variant="ghost" onClick={()=>setSelected(selected?.id===r.id?null:r)}>{selected?.id===r.id?<X className="mr-2 h-4 w-4"/>:<Eye className="mr-2 h-4 w-4"/>}{selected?.id===r.id?'Close detail':'Quick detail'}</Button></div>{selected?.id===r.id&&<div className="mt-4 rounded-xl bg-slate-50 p-4 text-sm"><div className="grid gap-3 sm:grid-cols-4"><div><span className="text-muted-foreground">Previous invoice</span><div className="font-semibold">{money(r.statement?.previous?.invoiceNet)}</div></div><div><span className="text-muted-foreground">Previous credit</span><div className="font-semibold">{money(r.statement?.previous?.creditNoteNet)}</div></div><div><span className="text-muted-foreground">Receipt (1M)</span><div className="font-semibold">{money(r.statement?.previous?.receiptNet)}</div></div><div><span className="text-muted-foreground">Current closing</span><div className="font-semibold">{money(r.statement?.current?.closingBalance)}</div></div></div></div>}</CardContent></Card>)}</div>{!shown.length&&!error&&<div className="py-16 text-center text-muted-foreground">No billing records found.</div>}</div>}
