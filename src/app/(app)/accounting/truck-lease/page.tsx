'use client';
import {useEffect,useMemo,useState} from 'react';import {auth} from '@/lib/firebase';import {getCurrentUser,SessionUser} from '@/lib/session';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';import {Loader2,Truck,CheckCircle2} from 'lucide-react';type Row={month:string;companyId:string;registeredName:string;status?:string;deductions?:{truckLease?:number};truckLease?:{acquisitionAmount?:number;amountPaid?:number;amountRemaining?:number}};const n=(v:any)=>Number(v||0);const money=(v:number)=>new Intl.NumberFormat('en-ZA',{style:'currency',currency:'ZAR'}).format(v||0);export default function TruckLeasePage(){const[user,setUser]=useState<SessionUser|null>(null),[rows,setRows]=useState<Row[]>([]),[loading,setLoading]=useState(true),[company,setCompany]=useState('');useEffect(()=>{(async()=>{try{const u=await getCurrentUser();setUser(u);if(u?.userType==='edo')setCompany(u.companyId||u.edoId||'');const a=auth.currentUser;if(!a)return;const token=await a.getIdToken();const res=await fetch('/api/month-end',{headers:{Authorization:`Bearer ${token}`},cache:'no-store'}),x=await res.json();if(res.ok)setRows((x.records||[]).filter((r:Row)=>String(r.status||'').toLowerCase()==='approved'))}finally{setLoading(false)}})()},[]);const companies=useMemo(()=>[...new Map(rows.map(r=>[r.companyId,r.registeredName])).entries()].sort((a,b)=>a[1].localeCompare(b[1])),[rows]);const scoped=rows.filter(r=>user?.userType==='edo'?r.companyId===company:!company||r.companyId===company).sort((a,b)=>b.month.localeCompare(a.month));const latest=scoped[0];const acquisition=n(latest?.truckLease?.acquisitionAmount);const paid=latest?.truckLease?.amountPaid!=null?n(latest.truckLease.amountPaid):scoped.reduce((s,r)=>s+n(r.deductions?.truckLease),0);const remaining=latest?.truckLease?.amountRemaining!=null?n(latest.truckLease.amountRemaining):Math.max(0,acquisition-paid);const done=acquisition>0&&remaining<=0;if(loading)return <div className="flex min-h-64 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin"/></div>;return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Truck Lease Repayment</h1><p className="text-muted-foreground">Premier vehicle acquisition and repayment position.</p></div>{user?.userType!=='edo'&&<Card><CardContent className="pt-6"><select className="h-10 w-full max-w-md rounded-md border bg-background px-3" value={company} onChange={e=>setCompany(e.target.value)}><option value="">Select EDO</option>{companies.map(([id,name])=><option key={id} value={id}>{name}</option>)}</select></CardContent></Card>}{latest&&<><Card><CardHeader><CardTitle className="flex items-center gap-2"><Truck className="h-5 w-5"/>{latest.registeredName}</CardTitle></CardHeader><CardContent><div className="grid gap-4 md:grid-cols-3"><div className="rounded-xl border p-4"><div className="text-sm text-muted-foreground">Acquisition Amount</div><div className="mt-1 text-2xl font-bold">{acquisition?money(acquisition):'Awaiting workbook data'}</div></div><div className="rounded-xl border p-4"><div className="text-sm text-muted-foreground">Amount Paid</div><div className="mt-1 text-2xl font-bold">{money(paid)}</div></div><div className="rounded-xl border p-4"><div className="text-sm text-muted-foreground">Amount Remaining</div><div className="mt-1 text-2xl font-bold">{acquisition?money(remaining):'—'}</div></div></div>{done&&<div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 p-5 text-emerald-800"><div className="flex items-center gap-2 font-bold"><CheckCircle2 className="h-5 w-5"/>Congratulations!</div><p className="mt-1 text-sm">Your Premier truck lease has been fully repaid. The acquisition balance is complete.</p></div>}</CardContent></Card><Card><CardHeader><CardTitle>Repayment History</CardTitle></CardHeader><CardContent><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b"><th className="p-2 text-left">Month</th><th className="p-2 text-right">Monthly Lease Repayment</th></tr></thead><tbody>{scoped.map(r=><tr key={`${r.month}-${r.companyId}`} className="border-b"><td className="p-2">{r.month}</td><td className="p-2 text-right font-medium">{money(n(r.deductions?.truckLease))}</td></tr>)}</tbody></table></div></CardContent></Card></>}{!latest&&<Card><CardContent className="py-12 text-center text-muted-foreground">Select an EDO to view truck lease information.</CardContent></Card>}</div>}
