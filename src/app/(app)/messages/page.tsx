'use client';
import {useEffect,useState} from 'react';import {auth} from '@/lib/firebase';import {Card,CardContent,CardHeader,CardTitle} from '@/components/ui/card';import {Loader2,MessageSquare} from 'lucide-react';
type Msg={id:string;title?:string;message?:string;createdAt?:string;createdByName?:string;expiresOn?:string|null;active?:boolean};
function stamp(v?:string){if(!v)return'';const d=new Date(v);return new Intl.DateTimeFormat('en-ZA',{dateStyle:'medium',timeStyle:'short'}).format(d)}
export default function MessagesPage(){const[items,setItems]=useState<Msg[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState('');useEffect(()=>{(async()=>{try{const current=auth.currentUser;if(!current)throw new Error('Please sign in again.');const token=await current.getIdToken();const response=await fetch('/api/messages',{headers:{Authorization:`Bearer ${token}`},cache:'no-store'});const result=await response.json();if(!response.ok)throw new Error(result.error||'Unable to load messages.');setItems(Array.isArray(result.messages)?result.messages:[])}catch(e){setError(e instanceof Error?e.message:'Unable to load messages.')}finally{setLoading(false)}})()},[]);return <div className="mx-auto max-w-4xl space-y-6"><div><h1 className="text-3xl font-bold">Messages</h1><p className="text-muted-foreground">Messages and notices from Taskraft Admin.</p></div>{loading?<div className="flex min-h-48 items-center justify-center"><Loader2 className="h-6 w-6 animate-spin"/></div>:error?<div className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>:items.length===0?<Card><CardContent className="flex min-h-40 items-center justify-center text-sm text-muted-foreground">No current admin messages.</CardContent></Card>:<div className="space-y-3">{items.map(m=><Card key={m.id}><CardHeader className="pb-2"><CardTitle className="flex items-center gap-2 text-lg"><span className="flex h-9 w-9 items-center justify-center rounded-full bg-amber-50 text-amber-700"><MessageSquare className="h-4 w-4"/></span>{m.title||'Admin message'}</CardTitle></CardHeader><CardContent><p className="whitespace-pre-wrap text-sm leading-6">{m.message}</p><p className="mt-4 text-xs text-muted-foreground">{m.createdByName||'Taskraft Admin'}{m.createdAt?` • ${stamp(m.createdAt)}`:''}</p></CardContent></Card>)}</div>}</div>}
